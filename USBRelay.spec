# Gnirehtet Relay Manager - Python Dependencies
#
# Runtime dependencies: None (uses only Python standard library)
# Build dependencies: PyInstaller for creating the Windows executable

# Build tool - only needed for packaging
pyinstaller>=6.0.0
